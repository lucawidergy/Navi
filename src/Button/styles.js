import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonR: {
    borderRadius: 35,
    borderWidth: 3,
    height: 150,
    width:70,
    justifyContent: 'center',
    alignContent: 'center',
    borderColor:'white'
  },
  buttonText:{
    fontSize: 40,
    textAlign: 'center',
    color:'white'
  },
});
